<h3>Trainer</h3>
<p>Name: <%= @trainer.name if @trainer.name %></p>
<p>Email: <%= @trainer.email if @trainer.email %></p>
<p>Pokemon</p>
	<% @trainer.pokemons.each do |pokemon| %>
		<p>Name: <%= pokemon.name %> Level: <%= pokemon.level %> Health: <%= pokemon.health %></p> <%= button_to "Damage!", damage_path(id: pokemon.id, tid: @trainer.id), :class => "button medium", :method => :patch %>
	<% end %>
	<% if @trainer.id == current_trainer.id %>
		<%= button_to 'Create a Pokemon!', new_path(), :class => "button medium", :method => :get %>
	<% end %>